export default class Cozinheiro {
  prepararPedido(pedido) {
    console.log("Pedido sendo preparado pelo cozinheiro.");
  }
}

// Essa é a Classe que permite que o cozinheiro está preparando o pedido do cliente.
